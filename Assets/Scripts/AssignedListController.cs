using UnityEngine;
using System.Collections;
using System;
using System.Collections.Generic;


public class AssignedListController : MonoBehaviour{
/*	
	List<int> farmerList = new List<int>();
	List<int> researcherList = new List<int>();
	List<int> workerList = new List<int>();

	int f = 0, r = 0, w = 0;
	if(Hunter.getJob() == "Farmer"){
		if(f < structureFarm.getLevel() * 3){
			farmerList.Add(Hunter.getSerial());
			f++;
		}
	}
	else if(Hunter.getJob() == "Researcher"){
		if(r < structureAcademy.getLevel() * 3){
			researcherList.Add(Hunter.getSerial());
			r++;
		}
	}
	else if(Hunter.getJob() == "Worker"){
		if(w < structureWorkshop.getLevel() * 3){
			workerList.Add(Hunter.getSerial());
			w++;
		}
	}

	/*`````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````*/
	/*
	public List<int> farmerList{
		get{
			return farmerList;
		}
	}

	public List<int> researcherList{
		get{
			return researcherList;
		}
	}
	public List<int> workerList{
		get{
			return workerList;
		}
	}*/
}